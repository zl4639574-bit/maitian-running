# 麦田守望长跑队 · 数据中心

一个可以**双击打开**的网页，把队里散落在 Excel / Word 里的资料汇成一个能看、能搜、能上传的界面。

---

## 一、怎么用

**双击 `index.html`**（推荐用 Edge 或 Chrome 打开）即可，不需要装任何东西、不需要联网。

顶部六个标签页：

| 标签 | 内容 |
|---|---|
| **总览** | 队徽、口号、人数统计、最近一次测速前五名、近几年战绩 |
| **成绩榜** | 每组测速/比赛一张榜。可切数据集、按项目/性别筛选、搜姓名学院、点表头排序，自动算配速 |
| **队员名册** | 按正式/预备/队服/高百/冬训营筛选，按学院筛选，搜名字；每个人显示他自己的最好成绩 |
| **上传成绩** | ① 手动录一条 ② 拖 Excel/CSV 批量导入 ③ 本机已传成绩（导出/复制/删除） |
| **照片墙** | 44 张照片，分类筛选，点击放大、左右键翻页 |
| **荣誉与资料** | 历年战绩时间线、高水平运动员 PB、数据来源说明、隐私说明 |

## 二、上传成绩怎么玩

**方式一：手动一条**——填姓名、项目（5000米/半马…）、成绩，点「添加到我的成绩」。
成绩写法随便：`18:35`、`1:23:22`、`18'35"`、`18.5`（分钟）都能认。

**方式二：批量拖表**——把队里现成的测速表 `.xlsx / .xls / .csv` 直接拖进虚线框。
- 会自动猜「表头在第几行 / 哪列是姓名 / 哪列是成绩」
- 界面里可以手动改这几项，右侧实时预览「原始成绩 → 识别成什么」
- 认不出的成绩行（DNS、缺、空）自动跳过并提示跳了几条

**导出**：本机记录可 →「复制成文本」（直接粘到微信/问卷）／「导出 CSV」／「导出 Excel」。

> ⚠️ 「我上传的成绩」只存在**这台电脑的这个浏览器**里，换设备或清缓存会丢。
> 要汇总成全队数据：导出表格 → 发队群/队长 → 队长把数据放进队里的正式表格 → 重跑提取脚本。

## 三、资料更新后怎么办

队里新增表格后，在 `麦田守望数据中心` 文件夹里重跑两个脚本（双击 `更新数据.bat` 最省事）：

```
C:\Users\15749\.venvs\bib\Scripts\python.exe 提取数据.py    # 读 Excel/Word → data/team-data.js
C:\Users\15749\.venvs\bib\Scripts\python.exe 生成图片.py    # 压缩照片 → images/
```

改完刷新网页即可。想验证有没有问题，打开 `_自检.html`，页面底部会打印一串自检结果。

**新增一份成绩表要接到页面上**：在 `提取数据.py` 里照着 `ds_track(...)` / `ds_gaobai(...)` 的写法加一行，指向新文件即可。

## 四、目录结构

```
麦田守望数据中心/
├─ index.html            网页本体（双击这个）
├─ 提取数据.py           从队伍资料里读数据 → data/team-data.js
├─ 生成图片.py           压缩照片 → images/
├─ 更新数据.bat          一键跑上面两个脚本
├─ _自检.html            自检页（排查用）
├─ data/team-data.js     自动生成的数据（别手改）
├─ assets/               样式、脚本、表格读写库（离线可用）
└─ images/               压缩后的照片 + photos.js
```

## 五、数据口径与注意事项

- **数据全部来自队伍资料文件夹**，没有任何编造。脚本读取的文件都写在「荣誉与资料 → 数据从哪里来」里。
- **不含学号和手机号**：原始表格里有，脚本提取时已经丢掉。名册只保留姓名、年级、学院/专业和成绩。
- 需要留意的几处（页面上也标注了）：
  - **冬训营测速**原表没写距离，按性别推定为男生 5000 米 / 女生 3000 米。
  - **高百总决赛测速**的距离（16 公里）和**高百分站赛前测速**（4 公里）是按原表配速反推的。
  - 2022.10 的 10 公里测速、2022.12 冬训营测速的年份，是按文件时间推断的。
  - 名册 173 人是**历年**记录（2023 正式/预备队员表 + 训练考核表 + 队服收集 + 高百阵容 + 测速参与者），不是当前人数。
  - 同名不同人、姓名写法不同（如「周庭艳 / 周婷艳」）没有合并，需要的话告诉我。
- 尚未纳入：校运会成绩册（2021—2024，`.doc` 老格式）、4—7 月跑量考勤、361° 月度统计、高百 2025 资格赛（原表是空白模板）。要加就说。

## 六、给手机看 / 让全队都能看

### 方式 A：临时公网链接（马上能用，无需注册）

双击 **`启动公网链接.bat`**，窗口里会出现一行网址：

```
https://xxxxxx.trycloudflare.com
```

把这个网址发到群里，任何手机用浏览器都能打开。

- 网址**每次启动都不一样**，关掉黑窗口 / 电脑睡眠关机，链接立刻失效。
- 页面是公开的：**拿到链接的人都能看**（内容里只有姓名、学院、成绩、照片，不含手机号学号）。
- 已在手机视口（390×844）实测：导航自动折行、表格不横溢、照片正常加载。

### 方式 B：永久网址（推荐，需要注册一个账号）

把整个文件夹传到静态托管上，就能得到固定网址。三个选择和取舍：

| 方案 | 费用 | 国内手机打开 | 需要什么 |
|---|---|---|---|
| **GitHub Pages** | 免费 | 一般可以，偶尔慢 | 注册 GitHub 账号（推荐先试这个） |
| 阿里云 OSS / 腾讯云 COS 静态网站 | 几毛钱/月流量 | 最快最稳 | 实名认证 + 域名备案（用默认地址可免备案） |
| Cloudflare Pages / Vercel | 免费 | 经常打不开 | 注册账号 |

数据更新后重新上传一次即可（命令行 `git push`，或者网页上直接拖文件）。

---

## 附：手机上怎么检查有没有排版问题

```
手机检查.py 390 --check           # 在 390px 宽的手机视口下找横向溢出
手机检查.py 390                    # 截图到 手机截图/ 文件夹
手机检查.py 390 --url https://...  # 检查任意网址（比如临时公网链接）
```

注意：浏览器会缓存 `file://` 的 css/js，改了样式看不到效果时，按 Ctrl+F5 强刷。
