# 麦田守望长跑队 · 数据中心（三个版本）

## 🌐 永久网址（手机直接打开，不用 VPN）

| 版本 | 网址 | 谁能改 |
|---|---|---|
| **展示版**（发群里用这个） | https://zl4639574-bit.github.io/maitian-running/ | 只读 |
| **队员版** | https://zl4639574-bit.github.io/maitian-running/member/ | 队员录自己的成绩 |
| **队长版** | https://zl4639574-bit.github.io/maitian-running/captain/ | 队长全权 |

仓库：https://github.com/zl4639574-bit/maitian-running （公开，改数据/改代码都在这里）

---
一个网页，三种用法，共用同一份数据：

| 版本 | 文件 | 谁能改 | 有什么 |
|---|---|---|---|
| **展示版** | `index.html` | 只读 | 总览 / 成绩榜 / 队员名册 / 照片墙 / 荣誉与资料。给全队和外面看 |
| **队员版** | `member/index.html` | 队员只能录自己的成绩 | 展示版 + 上传成绩（可录 5000米、半马、全马…），**没有** Excel 批量导入 |
| **队长版** | `captain/index.html` | 队长全权 | 队员版 + Excel 批量导入 + 比赛成绩管理 + 照片上传 + 队伍信息/荣誉/名册修改 + 一键同步到线上 |

数据来源：`E:\Desktop\麦田` 里的原始资料，由脚本自动抓取（见文末）。

---

## 一、三个版本都能看什么

**总览**　队徽、队名、口号、在册队员数 / 最好成绩条数 / 成绩记录数 / 相册数；最近一次测速前五名；4 个相册预览；近年战绩。

**成绩榜**（核心）
- 顶部一排胶囊：**个人最好成绩** + **每一场比赛/测速各一张榜**
- 点「个人最好成绩」：把队里所有比赛记录合起来，每个人每个项目只留最快一次，按项目分组（16公里 / 10000米 / 5000米 / 3000米 / 1500米…），自动算配速
- 点某一场比赛（如「2024.4.28 春季场地测速」）：这场比赛当天的原始名次榜
- **只有队长版能改比赛成绩**；队员和访客只能看

**队员名册**　只保留**正式队员（60 人）和预备队员（20 人）**，可按正式/预备、学院、姓名筛选；每人卡片显示他自己的最好成绩。

**照片墙**　**一个赛事一个相册**，点进去看这个相册的全部照片，点照片可放大、左右键翻页。

**荣誉与资料**　历年战绩时间线 + 高水平运动员个人最好成绩 + 队伍活动。（不包含队伍介绍图、数据来源、隐私说明）

## 二、队员怎么上传成绩（队员版）

1. 打开队员版 → 「上传成绩」
2. 先点一下项目：**5000米 / 3000米 / 10000米 / 半马 / 全马 / 其他**（半马、全马单独留了入口）
3. 填姓名、成绩（`18:35`、`1:23:22`、`18'35"`、`18.5` 都能认）、日期、名次、赛事名称
4. 点「添加到我的成绩」→ 会立刻出现在「成绩榜」里（带「本机」标记）

> ⚠️ 队员自己录的成绩只存在**他自己手机浏览器**里。要进全队榜单，点「复制成文本」发给队长，或者由队长在队长版里录入 / 导入。

## 三、队长怎么用（队长版）

**① 批量导入成绩**：上传成绩 → 把 Excel/CSV 拖进去（手机上点一下就能选文件，微信收到的表也行）→ 系统自动猜表头行和姓名/成绩列 → 确认后导入。

**② 归入某场比赛**：数据管理 → 比赛成绩 → 选一场比赛 → 「把『上传成绩』里的 N 条并入」；也可以直接新建比赛、一条条录、删掉某条成绩。

**③ 照片**：数据管理 → 照片 → 选相册（或新建）→ 选照片（可多选，浏览器自动压缩到长边 1500px）。

**④ 队伍信息 / 荣誉 / 名册**：数据管理里对应分区。名册可以改学院/专业/年级/身份，也可以把某人从公开名册删掉（可恢复）。

**⑤ 一键同步到线上**：数据管理 → 同步
1. 填 GitHub 用户名、仓库名、分支（默认 main）
2. 填访问令牌：GitHub 网页 → 头像 → Settings → Developer settings → Personal access tokens → **Fine-grained tokens** → 权限里 **Contents = Read and write** → 生成 `github_pat_...` 粘进去
3. 点「同步我的修改到线上」→ 约 1 分钟后**三个版本全部更新**

> 令牌只存在那台设备的浏览器里，不会写进网页，也不会被别人看到。
> 换个设备（比如用队长的手机）时，只要在那台设备上再填一次用户名/仓库/令牌即可，**不需要传任何文件**。

## 四、数据更新（改了队里的 Excel 之后）

双击 `更新数据.bat`（需在本机），它会：
1. `提取数据.py` —— 读 `E:\Desktop\麦田` 里的 Excel/Word → 生成 `data/team-data.js`
2. `生成图片.py` —— 压缩照片 → `images/`

改完刷新网页即可（网页里的数据是带时间戳加载的，不会读到旧缓存）。

**新增一场比赛/一次测速要接到网页上**：在 `提取数据.py` 里照着 `ds_track(...)` / `ds_gaobai(...)` 加一行指向新文件即可；或者干脆用队长版的「新建比赛 + 导入成绩」，不用动代码。

## 五、隐私

- **不含学号和手机号**：原始表格里有，提取时已经丢掉。
- 页面加了 `noindex`，不会被搜索引擎收录。
- 名册里的姓名、学院、成绩、照片都是真实信息，链接**发到群里等于对拿到链接的人公开**。要更保守的话可以：只显示现役队员 / 姓名改成「姓+名首字」——告诉我改。

## 六、数据口径（页面上也标注了）

- 冬训营测速原表没写距离，按性别推定男 5000 米 / 女 3000 米
- 高百总决赛测速 16 公里、分站赛前测速 4 公里，是按原表配速反推的
- 2022.10 的 10 公里测速、2022.12 冬训营测速的年份按文件时间推断
- 名册只保留标记为正式/预备的 74 人；其余 99 个历史名字（队服/高百/冬训营名单）已按需求隐藏
- 尚未纳入：校运会成绩册（2021—2024 老 `.doc`）、4—7 月跑量考勤、361° 月报

## 七、手机排版怎么自查

```
手机检查.py 390 --check            # 在 390px 手机视口下找横向溢出
手机检查.py 390                    # 截图存到 手机截图/
手机检查.py 390 --url https://...   # 检查线上网址
```

> 注意：浏览器会缓存 `file://` 的 css/js。改了样式看不到变化就按 Ctrl+F5；
> 线上版的数据和脚本都带时间戳加载，不会遇到这个问题。
